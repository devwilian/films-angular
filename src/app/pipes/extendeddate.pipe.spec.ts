import { ExtendeddatePipe } from './extendeddate.pipe';

describe('ExtendeddatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtendeddatePipe();
    expect(pipe).toBeTruthy();
  });
});
